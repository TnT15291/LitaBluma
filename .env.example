# Supabase (frontend) — safe to expose; the anon key is gated by RLS.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Service-role keys and AI provider keys belong ONLY in the backend proxy /
# Supabase Edge Function environment — never add them here or in the browser.
